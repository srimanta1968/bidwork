{
  "endpoint": "/api/bids/:bidId/additional-work/:awoId/reject",
  "method": "POST",
  "taskId": "049fa826-1642-43f3-829a-50725e7aedc2",
  "description": "Homeowner rejects an additional work order with required notes",
  "requiresAuth": true,
  "priority": 75,
  "dependsOn": ["POST /api/bids/:bidId/additional-work"],
  "pathParams": {
    "bidId": "{{cache:bids.create.response.data.bid.id}}",
    "awoId": "{{cache:additional_work.submit.response.data.additional_work.id}}"
  },
  "testability": "manual",
  "testCases": [{
    "name": "Reject additional work",
    "headers": {"Authorization": "Bearer {{cache:auth.register.response.data.token}}"},
    "payload": {
      "owner_response_notes": "Out of scope; not needed for this project."
    },
    "expectedStatus": 200,
    "expectedResponse": {"success": true}
  }]
}
