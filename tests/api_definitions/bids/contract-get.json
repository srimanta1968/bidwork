{
  "endpoint": "/api/bids/:id/contract",
  "method": "GET",
  "taskId": "d97b0c86-6d65-41e8-9dca-8ac472311d7e",
  "description": "Fetch the contract draft + signatures for a bid (owner + contractor only)",
  "requiresAuth": true,
  "priority": 92,
  "dependsOn": ["POST /api/bids/:id/accept-offer"],
  "pathParams": {"id": "{{cache:bids.create.response.data.bid.id}}"},
  "testability": "manual",
  "testCases": [{
    "name": "Get contract",
    "headers": {"Authorization": "Bearer {{cache:auth.register.response.data.token}}"},
    "expectedStatus": 200,
    "expectedResponse": {"success": true}
  }]
}
